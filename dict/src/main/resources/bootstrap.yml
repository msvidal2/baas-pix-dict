spring:
    application:
        name: dict
    cloud:
        config:
            uri: ${CONFIG_SERVER_URI:http://localhost:8888}
            name: dict,dict-bacen,dict-database,dict-messaging
    profiles:
        active:
            - ${ENV:local}
            - bacen-${ENV:local}
            - database-${ENV:local}
            - messaging-${ENV:local}
            - cloudtask-${ENV:local}

