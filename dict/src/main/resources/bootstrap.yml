spring:
    application:
        name: dict
    cloud:
        config:
            uri: http://localhost:8888
            name: dict,dict-bacen,dict-database
    profiles:
        active:
            - local
            - bacen-local
            - database-local

