spring:
    application:
        name: dict-polling-claim
    cloud:
        config:
            uri: http://localhost:8888
            name: dict-polling-claim, dict
    profiles:
        active:
            - local
            - bacen-local
            - database-local
            - messaging-local
