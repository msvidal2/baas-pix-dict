spring:
    application:
        name: dict-polling-claim
    main:
        allow-bean-definition-overriding: true
        banner-mode: off
    cloud:
        config:
            uri: ${CONFIG_SERVER_URI:http://localhost:8888}
            name: dict-overdue-possession-claim,dict-bacen,dict-database,dict-messaging
    profiles:
        active:
            - ${ENV:local}
            - bacen-${ENV:local}
            - database-${ENV:local}
            - messaging-${ENV:local}
            - cloudtask-${ENV:local}
