spring:
    application:
        name: dict-polling-claim
    main:
        allow-bean-definition-overriding: true
        banner-mode: off
    cloud:
        config:
            uri: ${CONFIG_SERVER_URI:http://localhost:8888}
            name: dict-polling-claim,dict-bacen,dict-database
    profiles:
        active:
            - ${ENV:local}
            - bacen-${ENV:local}
            - database-${ENV:local}
            - cloudtask-${ENV:local}
